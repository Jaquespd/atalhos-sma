import React from 'react';

import { Container, Content } from './styles';

export default function Divisor({ title }) {
  return (
    <Container>
      <Content>
        <h1>{title}</h1>
      </Content>
    </Container>
  );
}
